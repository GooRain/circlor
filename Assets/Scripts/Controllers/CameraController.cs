using UnityEngine;

public class CameraController : MonoBehaviour
{
	private void Awake()
	{

		//widthRatio = Screen.width / prefferedWidth;
		//heightRatio = Screen.height / prefferedHeight;
		//Screen.SetResolution(prefferedWidth, prefferedHeight, true);

		//myCamera = GetComponent<Camera>();
	}
}
